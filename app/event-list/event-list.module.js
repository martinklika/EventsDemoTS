var app;
(function (app) {
    var eventList;
    (function (eventList) {
        'use strict';
        angular.module('app.eventList', ['ngRoute', 'ngResource']);
    })(eventList = app.eventList || (app.eventList = {}));
})(app || (app = {}));
